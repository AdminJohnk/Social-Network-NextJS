import * as React from 'react';

export interface ICurrentConversationProps {
  params: { conversationId: string};

}

export default function CurrentConversation ({params: {conversationId}}: ICurrentConversationProps) {
    
  return (
    <div>
      
    </div>
  );
}
